# SMTP configuration for admin email alerts
SMTP_HOST=smtp.yourhost.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=puller@zileva.online
SMTP_PASS=your-email-password
# Address the alert emails are sent from (optional; defaults to SMTP_USER)
EMAIL_FROM="USDT Puller <puller@zileva.online>"
# Destination for alert emails (defaults to EMAIL_FROM if not set)
ADMIN_EMAIL=admin@yourdomain.com
